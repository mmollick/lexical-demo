/**
 * Toolbar icons, mapped to Lucide.
 *
 * Lucide is open source (ISC) and covers every glyph the toolbar needs. The
 * exported names are kept stable so the call sites in plugins/Toolbar are
 * insulated from the icon set behind them.
 */
export {
  AlignCenter as AlignmentCenter,
  AlignJustify as AlignmentJustified,
  AlignLeft as Alignment,
  AlignLeft as AlignmentLeft,
  AlignRight as AlignmentRight,
  Baseline as Fontcolor,
  Bold,
  Highlighter as BgColor,
  Image as AddImage,
  IndentDecrease as IndentLeft,
  IndentIncrease as Indent,
  IndentIncrease as IndentRight,
  Italic,
  Link as AddLink,
  List,
  ListOrdered as NumList,
  Merge,
  Plus as Add,
  Redo2 as Redo,
  Settings as Setting,
  Sparkles as Ai,
  SquarePlus as AddButton,
  Type as Styles,
  Underline,
  Undo2 as Undo,
} from 'lucide-react';
