/**
 * The rendered-document styles the editor reads, kept here so the in-editor
 * button preview matches the sent output.
 */
export const styles = {
  button: {
    backgroundColor: '#4C8BEA',
    borderRadius: '8px',
    color: '#FFFFFF',
    fontSize: '14px',
    lineHeight: '100%',
    margin: '1em 0',
    padding: '12px 20px',
  },
  img: {
    display: 'block',
    margin: '1em auto',
    maxWidth: '100%',
  },
};
